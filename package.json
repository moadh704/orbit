{
  "name": "orbit",
  "version": "1.0.0",
  "private": true,
  "description": "Orbit — team project management (Vue 3 + Express + MySQL + Socket.io)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "dev": "echo Use client and server dev scripts separately",
    "migrate": "node server/db/migrate.js"
  },
  "engines": {
    "node": ">=18"
  }
}
